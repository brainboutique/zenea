## Sandbox
- The agent sandbox has no internet connectivity.
- Tell me which packages to install and I will take care of this.

## Build
- Builds are done outside the Sandbox the agent runs in.
- For validation, run lint and tsbuild but do not attempt full esbuild (as tools are not installed)